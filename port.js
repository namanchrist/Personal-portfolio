var options = {
    strings: ["Frontend Developer", "Trekker", "Vlogger", "Web Developer"],
    typeSpeed: 50,
    backSpeed: 90,
    backDelay: 1000,
    loop: true
};

var typed = new Typed(".text", options);
